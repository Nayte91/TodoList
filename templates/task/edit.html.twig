{% extends 'base.html.twig' %}

{% block title %}{{ task.title }}{% endblock %}

{% block header_title %}
    <h1>Editer la tâche <i>"{{ task.title }}"</i></h1>
{% endblock %}

{% block header_img %}
    <img class="slide-image" src="{{ asset('assets/img/todolist_content.jpg') }}" alt="todo list">
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            {{ form_start(form, {'action' : path('task_edit', { 'id' : task.id })}) }}
                {{ form_widget(form) }}

                <button type="submit" class="btn btn-success pull-right">Modifier</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
